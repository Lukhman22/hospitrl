[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build-meta"

[project]
name = "hospitrl"
version = "1.0.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "openai",
    "gymnasium",
    "openenv-core>=0.2.0"
]

[project.scripts]
hospitrl-server = "server.app:main"